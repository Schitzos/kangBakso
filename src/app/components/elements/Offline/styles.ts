import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 16,
    gap: 8,
  },
  imageContainer: {
    width: '100%',
    height: 150,
    justifyContent: 'flex-end',
    alignItems: 'center',
    display: 'flex',
  },
  image: {
    width: 150,
    height: 150,
  },
});
